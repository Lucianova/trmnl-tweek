# Local API-testing scripts with real credentials — never commit
# (whole folder ignored so any new file dropped in is covered too)
/tweek fetch examples/
fetchTasks.ts
refreshToken.ts
requestIdToken.ts
fetchCalendars.ts

# Internal planning/spec docs
docs/superpowers/

# Dependencies
node_modules/

# trmnlp binary
tweek/bin/

# trmnlp build output
tweek/_build/
